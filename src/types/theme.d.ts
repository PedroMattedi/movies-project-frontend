export type ThemeMode = "light" | "dark";

export interface Theme {
  background: string;
  layoutColor: string;
  layoutBorderColor: string;
  text: string;
  modalWrapper: string;
  themeButton: string;
  mainButton: string;
  mainButtonText: string;
}
