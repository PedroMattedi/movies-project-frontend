import { colors } from "../constants/colors";
import type { Theme } from "../types/theme";

const light: Theme = {
  background: colors.mauve[1],
  text: "",
  modalWrapper: "",
  themeButton: colors.purpleAlpha[2],
};

export default light;
